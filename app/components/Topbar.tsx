const Topbar = () => {
  return (
    <section className="flex h-16 items-center justify-between border-b px-6">
      <h1 className="text-lg font-semibold">Dashboard</h1>
      <div>User</div>
    </section>
  );
};

export default Topbar;
